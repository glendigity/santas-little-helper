# Generated test output (per-project, gitignored)
slh-reports/

# Note: .slh/ is NOT gitignored — it contains profiles, knowledge,
# and orientation that should be committed and shared with the team.

# OS
.DS_Store
Thumbs.db
.claude/settings.local.json
